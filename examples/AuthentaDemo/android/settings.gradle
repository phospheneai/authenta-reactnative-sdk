pluginManagement {
	includeBuild("../node_modules/@react-native/gradle-plugin")
}

plugins {
	id("com.facebook.react.settings")
}

// Needed so buildscript classpath can resolve `com.facebook.react:react-native-gradle-plugin`
// from the local included build instead of Maven.
includeBuild("../node_modules/@react-native/gradle-plugin")

reactSettings {
	autolinkLibrariesFromCommand()
}

rootProject.name = 'AuthentaDemo'
include ':app'
